Drop royalty-free background tracks here as background_1.mp3 (etc).
If this folder is empty, mix_audio() in app/tools.py auto-generates a silent
10-minute placeholder track instead — your videos will just have no music.
